# launching the harvester with streaming API mode.

java -jar harvester.jar h1.config couchdb.properties twitter4j-1.properties

# launching the harvester with search API mode.

java -jar harvester.jar h2.config couchdb.properties twitter4j-2.properties
